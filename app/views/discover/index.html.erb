<%= button_to 'Top Rated Movies', "users/#{@user.id}/movies"  %>

<%= form_with(url: user_discover_index_path(@user), method: :get) do |form| %>
  <%= form.label :Find_movie_by_title %>
  <%= form.text_field :Find_movie_by_title, placeholder: "Search Movie By Title" %>

  <%= button_to 'Search Movie', "users/#{@user.id}/movies"  %>
<% end %>

